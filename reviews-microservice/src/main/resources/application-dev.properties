# Database configuration (in-memory H2; the schema is recreated on every start)
jdbc.driverClassName=org.h2.Driver
jdbc.url=jdbc:h2:mem:reviews;DB_CLOSE_DELAY=-1

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=create-drop
